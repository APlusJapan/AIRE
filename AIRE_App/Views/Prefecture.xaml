<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:ViewModels="clr-namespace:AIRE_App.ViewModels"
             x:DataType="ViewModels:PrefectureViewModel"
             x:Class="AIRE_App.Views.PrefectureView">

    <ScrollView>
        <ListView ItemsSource="{Binding Items}" ItemTapped="OnItemTapped_Item">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="{x:Type ViewModels:ItemViewModel}">
                    <ViewCell>
                        <VerticalStackLayout>
                            <HorizontalStackLayout>
                                <CheckBox Opacity="0" InputTransparent="True"/>
                                <Label Text="{Binding Name}" VerticalTextAlignment="Center"/>
                            </HorizontalStackLayout>
                        </VerticalStackLayout>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </ScrollView>

</ContentPage>
